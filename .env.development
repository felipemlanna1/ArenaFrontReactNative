# Arena Mobile - Development Environment
# Este arquivo é para desenvolvimento local com backend rodando

# API Configuration
# IMPORTANTE: Use seu IP local para testar em dispositivos físicos
# Para encontrar seu IP: Mac/Linux: ifconfig | grep "inet " | Windows: ipconfig
API_URL=http://localhost:3000/api/v1
EXPO_PUBLIC_API_URL=http://localhost:3000
API_TIMEOUT=30000

# Authentication
AUTH_TOKEN_KEY=@arena:auth_token

# Analytics (disabled in development)
ANALYTICS_ENABLED=false
ANALYTICS_KEY=

# Environment
ENVIRONMENT=development
NODE_ENV=development

# Mobile Development Tips:
# 1. Simulador iOS/Android: use localhost
# 2. Dispositivo físico na mesma rede: use seu IP local (ex: 192.168.1.100)
# 3. Para descobrir IP local:
#    - Mac: ifconfig | grep "inet " | grep -v 127.0.0.1
#    - Linux: ip addr show | grep "inet " | grep -v 127.0.0.1
#    - Windows: ipconfig | findstr "IPv4"
