# Arena Mobile - Development on Physical Device
# Use este arquivo quando estiver testando em dispositivo físico
# Copie para .env quando for usar

# API Configuration
# SUBSTITUA 192.168.1.100 pelo SEU IP LOCAL
# Para descobrir: ifconfig | grep "inet " (Mac/Linux) ou ipconfig (Windows)
API_URL=http://192.168.1.100:3000/api/v1
EXPO_PUBLIC_API_URL=http://192.168.1.100:3000
API_TIMEOUT=30000

# Authentication
AUTH_TOKEN_KEY=@arena:auth_token

# Analytics (disabled in development)
ANALYTICS_ENABLED=false
ANALYTICS_KEY=

# Environment
ENVIRONMENT=development
NODE_ENV=development

# IMPORTANTE:
# 1. Certifique-se de que seu backend está rodando
# 2. Certifique-se de que seu celular está na mesma rede Wi-Fi
# 3. Substitua 192.168.1.100 pelo seu IP local real
# 4. Para usar: cp .env.development.device .env
